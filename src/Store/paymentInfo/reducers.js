const defaultState = {
        paymentDescription: "",
        paymentSum: "",
        paymentDate: "",
        paymentStatus: "",
        paymentReceiver: ""
};

const paymentInfoReducer = (state, action) => {
    switch (action.type) {
        case ...
    
        return state;
    }
};