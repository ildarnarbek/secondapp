const defaultState = {
        paymentDescription: "",
        paymentSum: "",
        paymentDate: "",
        paymentStatus: "",
        paymentReceiver: ""
        }
};

const paymentInfoReduser = (state, action) => {
    switch (action.type) {
        case ...
    
        return state;
    }
};